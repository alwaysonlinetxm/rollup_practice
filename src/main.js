import React from 'react';
import ReactDOM from 'react-dom';

import Root from './Root';
import './reset.scss';

ReactDOM.render(
  <Root />,
  document.querySelector('#app')
);


// document.querySelector('#app').innerHTML = Root;
